const Header = () => {
  return (
    <nav className="navbar">
      <button>{/* <Link to={"/registration.js"}>Registrati</Link> */}</button>
      <button>Login</button>
    </nav>
  );
};

export default Header;
